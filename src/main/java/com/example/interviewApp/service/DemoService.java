package com.example.interviewApp.service;

import com.example.interviewApp.model.Runner;

public interface DemoService {

    String firstUniqueProduct(String[] products);
    void getTasks(Runner runner);
}
